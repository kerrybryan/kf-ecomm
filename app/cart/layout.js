export const metadata = {
  title: 'Shopping Bag',
  description: 'View and manage items in your KB Furniture shopping bag.',
};

export default function CartLayout({ children }) {
  return children;
}
