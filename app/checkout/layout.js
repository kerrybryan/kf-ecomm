export const metadata = {
  title: 'Secure Checkout',
  description: 'Complete your order with Cash on Delivery or Telebirr bank transfer.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
